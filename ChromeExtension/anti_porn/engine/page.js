// function loadScript(url){
	// var v = document.createElement('script');
	// v.src=url;
	// v.type='text/javascript';
	// (document.head||document.documentElement).appendChild(v);
// }
// // alternative to DOMContentLoaded
// document.onreadystatechange = function () {
    // if (document.readyState == "complete") {
		 // if (window !== window.top) 
			// return;
        // loadScript('engine/jquery-1.11.1.min.js');
		// des = $('meta[name="description"]').attr('content');
		
		// keywords = $('meta[name="keywords"]').attr('content');
		
		// des = des.toLowerCase();
		// keywords = keywords.toLowerCase();
		// alert(des);
		// alert(keywords);
		// if (-1 != des.indexOf('pháp luật') ||  -1 != keywords.indexOf('pháp luật'))
		// {
			// document.url = 'http://www.gmail.com';
		// }
		
		
    // }
// }